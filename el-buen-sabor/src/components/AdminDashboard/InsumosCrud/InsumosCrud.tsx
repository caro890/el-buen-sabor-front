import { useLoaderData } from "react-router"

export const InsumosCrud = () => {
  //const insumos = useLoaderData()

  return (
    <div>InsumossCrud</div>
  )
}

//loader function
export const insumosLoader = async () => {
  //const res = await fetch(url)
  //return res.json()
}