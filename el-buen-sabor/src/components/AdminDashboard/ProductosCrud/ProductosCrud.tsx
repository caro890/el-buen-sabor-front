import { useLoaderData } from "react-router"

export const ProductosCrud = () => {
  //const productos = useLoaderData()

  return (
    <div>ProductosCrud</div>
  )
}

//loader function
export const productosLoader = async () => {
  //const res = await fetch(url)
  //return res.json()
}